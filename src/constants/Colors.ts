const primary = '#41403e';
const secondary = '#0071de';
const success = '#86a361';
const warning = '#ddcd45';
const danger = '#a7342d';
const muted = '#868e96';

const colors: { [key: string]: string } = {
  primary,
  secondary,
  success,
  warning,
  danger,
  muted,
  primaryColorLight: `${primary}7f`,
};

export default colors;
